# V19.1 Admin Load Hotfix

Fixes the V19 Trivia Admin startup error `categories is not defined`.

- Media dashboard now loads category population before rendering the dynamic category filter.
- Admin JS/CSS asset query version bumped to `19.1` to avoid stale browser cache.
- No Supabase migration is required.
