# Trivia V19 — Category Command Center

- Admin Question Explorer now shows live category population, difficulty distribution, photo coverage, and one-click Add Questions jobs.
- Export a complete Category Dashboard snapshot for the local Content Studio.
- Content Studio can load category snapshots/jobs and run acquisition + media resolution for one category in a single operation.
- Smart difficulty mode targets the thinnest difficulty bucket from the exported live counts.
- Custom/dynamic categories fall back to Topic Discovery when no fixed generator exists.
- Import the resulting normal Content Package through Trivia Admin.

Run migration `019_category_command_center.sql`.
