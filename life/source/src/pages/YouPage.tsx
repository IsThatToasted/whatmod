import { Bell, ChevronRight, CircleUserRound, MapPin, Moon, Settings, ShieldCheck } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import { useAppData } from '../contexts/AppDataContext'
import { useAuth } from '../contexts/AuthContext'
export default function YouPage(){const {profile,items,places}=useAppData();const {demo}=useAuth();const nav=useNavigate();const done=items.filter(i=>i.status==='completed').length;return <div className="page"><header className="profile-hero"><div className="avatar-large">{(profile?.greeting_name||'J')[0].toUpperCase()}</div><div><span className="eyebrow">YOU</span><h1>{profile?.display_name||profile?.greeting_name||'JustGlance user'}</h1><p>{demo?'Demo mode · connect Supabase to use a real account':profile?.email}</p></div></header><div className="snapshot"><div><strong>{done}</strong><span>completed</span></div><div><strong>{items.filter(i=>i.status==='open').length}</strong><span>waiting</span></div><div><strong>{places.length}</strong><span>places</span></div></div><section className="settings-list"><button onClick={()=>nav('/settings')}><CircleUserRound/><div><strong>Profile & schedule</strong><span>Name, wake time, work hours</span></div><ChevronRight/></button><button onClick={()=>nav('/settings')}><Bell/><div><strong>Notifications</strong><span>Briefs, reminders, shared activity</span></div><ChevronRight/></button><button onClick={()=>nav('/settings')}><MapPin/><div><strong>Places</strong><span>Home, work, stores, and context</span></div><ChevronRight/></button><button onClick={()=>nav('/settings')}><Moon/><div><strong>Appearance</strong><span>Light, dark, or system</span></div><ChevronRight/></button><button onClick={()=>nav('/settings')}><ShieldCheck/><div><strong>Privacy & data</strong><span>Export, location choices, account</span></div><ChevronRight/></button><button onClick={()=>nav('/settings')}><Settings/><div><strong>All settings</strong><span>Fine tune JustGlance</span></div><ChevronRight/></button></section></div>}
