import { Archive, CalendarDays, ContactRound, FolderKanban, Inbox, Lightbulb, ListChecks, Settings, ShoppingBasket, UsersRound } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
const cards=[['/tasks','Tasks','Today, upcoming, waiting, overdue and everything else.',ListChecks],['/shopping','Shopping','Groceries, wish lists and product links across your Spaces.',ShoppingBasket],['/inbox','Inbox','Triage things you captured before you knew where they belonged.',Inbox],['/memory','Memory','Your durable archive of photos, files, links and original captures.',Archive],['/projects','Projects','Outcomes with connected next actions.',FolderKanban],['/planner','Planner','Appointments, deadlines and reminders on one agenda.',CalendarDays],['/thoughts','Thoughts','Ideas and notes that do not need to become tasks.',Lightbulb],['/contacts','Contacts','People, phone numbers, addresses and birthdays connected to your captures.',ContactRound],['/spaces','Spaces','Invite people once, then control which categories they can view or edit.',UsersRound],['/settings','Settings','Preferences, places, notifications and account.',Settings]] as const
export default function OrganizePage(){const nav=useNavigate();return <div className="page"><header className="page-header"><div><span className="eyebrow">ORGANIZE</span><h1>Everything has a home.</h1><p>These are the deeper views. Capture stays fast; organization stays optional.</p></div></header><div className="organize-grid">{cards.map(([to,title,description,Icon])=><button key={to} onClick={()=>nav(to)}><Icon/><span><strong>{title}</strong><span>{description}</span></span></button>)}</div></div>}
