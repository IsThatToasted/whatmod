import { AddressBook, Archive, CalendarDays, FolderKanban, Inbox, Lightbulb, ListChecks, Settings, UsersRound } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
const cards=[['/tasks','Tasks','Today, upcoming, waiting, overdue and everything else.',ListChecks],['/inbox','Inbox','Triage things you captured before you knew where they belonged.',Inbox],['/memory','Memory','Your durable archive of photos, files, links and original captures.',Archive],['/projects','Projects','Outcomes with connected next actions.',FolderKanban],['/planner','Planner','Appointments, deadlines and reminders on one agenda.',CalendarDays],['/thoughts','Thoughts','Ideas and notes that do not need to become tasks.',Lightbulb],['/contacts','Contacts','People, phone numbers, addresses and birthdays connected to your captures.',AddressBook],['/spaces','Spaces','Shared household and collaborative lists.',UsersRound],['/settings','Settings','Preferences, places, notifications and account.',Settings]] as const
export default function OrganizePage(){const nav=useNavigate();return <div className="page organizer-page"><header className="page-header"><div><span className="eyebrow">ORGANIZE</span><h1>Everything has a home. You do not have to remember where.</h1><p>Use the structure when you want it; JustGlance still works when you only brain-dump and move on.</p></div></header><div className="organize-grid">{cards.map(([to,title,desc,Icon])=><button key={to} onClick={()=>nav(to)}><Icon/><div><strong>{title}</strong><span>{desc}</span></div></button>)}</div></div>}
