import { ArrowLeft, CheckCircle2, Copy, Link2, Plus, ShoppingBasket, Users, UserPlus } from 'lucide-react'
import { useMemo, useState } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { useAppData } from '../contexts/AppDataContext'
import { ItemCard } from '../components/ItemCard'
import { lifeIntentParser } from '../lib/parser'
import { Modal } from '../components/Modal'

export default function SpacePage(){
  const {id}=useParams();const nav=useNavigate();const {spaces,members,items,createItem,activity,createInvite}=useAppData();const space=spaces.find(s=>s.id===id)
  const [text,setText]=useState('');const [inviteOpen,setInviteOpen]=useState(false);const [email,setEmail]=useState('');const [inviteLink,setInviteLink]=useState('');const [inviteError,setInviteError]=useState('')
  const spaceMembers=members.filter(m=>m.space_id===id);const relevant=useMemo(()=>items.filter(i=>i.space_id===id&&i.status==='open'),[items,id]);const shop=relevant.filter(i=>i.type==='shopping');const other=relevant.filter(i=>i.type!=='shopping')
  async function add(){if(!text.trim()||!id)return;const p=lifeIntentParser.parse(text);await createItem(p,text,{space_id:id});setText('')}
  async function makeInvite(){if(!id)return;setInviteError('');try{const token=await createInvite(id,email.trim()||undefined);setInviteLink(`${location.origin}/life/#/invite/${encodeURIComponent(token)}`)}catch(e){setInviteError(e instanceof Error?e.message:'Could not create invite.') }}
  if(!space)return <div className="page"><div className="large-empty"><h2>That space isn’t available.</h2><button className="secondary-button" onClick={()=>nav('/spaces')}>Back to spaces</button></div></div>
  return <div className="page"><button className="text-button" onClick={()=>nav('/spaces')}><ArrowLeft size={16}/>Spaces</button><header className="page-header compact"><div><span className="eyebrow">SHARED SPACE</span><h1>{space.name}</h1><p><Users size={16}/> {space.role||'member'} access · updates sync in realtime</p></div>{space.role!=='member'&&<button className="secondary-button" onClick={()=>setInviteOpen(true)}><UserPlus size={17}/>Invite</button>}</header><div className="space-detail-grid"><section><div className="quick-add"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')add()}} placeholder="Add milk, take trash out, remind us…"/><button className="primary-button square" onClick={add} disabled={!text.trim()}><Plus/></button></div><div className="section-heading inline"><h2><ShoppingBasket size={18}/> Shopping</h2><span>{shop.length}</span></div>{shop.length?shop.map(i=><ItemCard key={i.id} item={i}/>):<div className="empty-row"><CheckCircle2 size={16}/>Your shared list is clear.</div>}<div className="section-heading inline top-gap"><h2>Everything else</h2><span>{other.length}</span></div>{other.length?other.map(i=><ItemCard key={i.id} item={i}/>):<div className="empty-row">No shared tasks waiting.</div>}</section><aside className="context-panel"><div className="context-card"><strong>Members</strong><div className="member-list">{spaceMembers.map(m=><div className="member-row" key={m.user_id}><span className="member-avatar">{(m.greeting_name||m.display_name||'M')[0].toUpperCase()}</span><span><b>{m.greeting_name||m.display_name}</b><small>{m.role}</small></span></div>)}</div></div><div className="context-card"><strong>Recent activity</strong>{activity.filter(a=>a.space_id===id).slice(0,5).map(a=><p key={a.id}>{a.actor_name||'Someone'} {a.action} <b>{a.entity_title}</b></p>)}{activity.length===0&&<p>Activity will appear here as members make changes.</p>}</div><div className="context-card"><strong>Sharing</strong><p>Owners and admins can create email-bound or secure-link invites. Personal spaces can never be shared.</p></div></aside></div><Modal open={inviteOpen} onClose={()=>{setInviteOpen(false);setInviteLink('');setInviteError('')}} title={`Invite to ${space.name}`}><div className="form-stack">{!inviteLink?<><label>Email (optional)<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="person@example.com"/></label><p className="muted">Leave email blank to create a secure share link. Email delivery is not sent by the browser; copy the generated link into the message service you prefer.</p>{inviteError&&<div className="form-message">{inviteError}</div>}<button className="primary-button" onClick={makeInvite}><Link2 size={17}/>Create invite link</button></>:<><label>Secure invite link<input value={inviteLink} readOnly/></label><button className="primary-button" onClick={async()=>{await navigator.clipboard.writeText(inviteLink)}}><Copy size={17}/>Copy link</button><p className="muted">This token is stored only as a hash in Supabase and expires after seven days.</p></>}</div></Modal></div>
}
