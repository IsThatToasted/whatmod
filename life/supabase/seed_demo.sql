-- Demo data is intentionally not inserted into production accounts.
-- The frontend's VITE_DEMO_MODE uses isolated in-memory/local sample data instead.
select 'JustGlance demo data lives only in the frontend demo provider.' as note;
