import { Activity, Bell, MapPin, Moon, Settings, Sun } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useLife } from '../providers/LifeProvider';
export function YouPage(){const {profile,items,places,activity}=useLife();const done=items.filter(i=>i.status==='completed').length;return <div className="page"><header className="profile-head"><div className="avatar">{(profile?.greeting_name||'J')[0].toUpperCase()}</div><div><span className="eyebrow">Your JustGlance</span><h1>{profile?.name||'Profile'}</h1><p>{profile?.email}</p></div></header><div className="snapshot-grid"><div><b>{done}</b><span>completed</span></div><div><b>{items.filter(i=>i.status!=='completed').length}</b><span>open</span></div><div><b>{places.length}</b><span>places</span></div></div><section><div className="section-head"><div><span className="section-kicker">Your rhythm</span><h2>A few useful defaults</h2></div></div><div className="settings-list"><div><Sun/><span><b>Wake time</b><small>{profile?.wake_time||'07:00'}</small></span></div><div><Moon/><span><b>Sleep time</b><small>{profile?.sleep_time||'23:00'}</small></span></div><div><MapPin/><span><b>Saved places</b><small>{places.length} configured</small></span></div><div><Bell/><span><b>Notifications</b><small>Architecture ready</small></span></div></div></section><section><div className="section-head"><div><span className="section-kicker">Recent activity</span><h2>Quiet history</h2></div></div><div className="activity-list">{activity.map(a=><div key={a.id}><Activity size={14}/><span>{a.text}</span></div>)}</div></section><Link className="secondary button-link" to="/settings"><Settings size={18}/>Open settings</Link></div>}
