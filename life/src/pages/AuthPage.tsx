import { Mail, Sparkles } from 'lucide-react'
import { useState } from 'react'
import { useAuth } from '../contexts/AuthContext'
export default function AuthPage(){const {signIn,signUp,sendMagicLink,resetPassword,signInWithGoogle}=useAuth();const [mode,setMode]=useState<'login'|'signup'>('login');const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(){setBusy(true);setMessage('');const err=mode==='login'?await signIn(email,password):await signUp(email,password,name);setMessage(err|| (mode==='signup'?'Check your email to verify your account.':''));setBusy(false)}return <div className="auth-page"><section className="auth-brand-panel"><div className="brand-orb xl">J</div><span className="eyebrow">JUSTGLANCE</span><h1>Your life,<br/>at a glance.</h1><p>Capture it once. Let the right thing surface when it matters.</p><div className="auth-benefit"><Sparkles/>No giant dashboards. No guilt. Just what’s useful now.</div></section><section className="auth-card"><div><h2>{mode==='login'?'Welcome back.':'Make life lighter.'}</h2><p>{mode==='login'?'See what matters right now.':'Create your account in under a minute.'}</p></div>{mode==='signup'&&<label>What should we call you?<input value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></label>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete={mode==='login'?'current-password':'new-password'}/></label>{message&&<div className="form-message">{message}</div>}<button className="primary-button full" onClick={submit} disabled={busy||!email||!password||(mode==='signup'&&!name)}>{busy?'Working…':mode==='login'?'Sign in':'Create account'}</button><div className="divider"><span>or</span></div><button className="secondary-button full" onClick={async()=>setMessage((await signInWithGoogle())||'')}>Continue with Google</button><button className="secondary-button full" onClick={async()=>{if(!email)return setMessage('Enter your email first.');setMessage((await sendMagicLink(email))||'Magic link sent.')}}><Mail size={17}/>Email me a magic link</button><div className="auth-links"><button onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'Create an account':'I already have an account'}</button>{mode==='login'&&<button onClick={async()=>{if(!email)return setMessage('Enter your email first.');setMessage((await resetPassword(email))||'Password reset link sent.')}}>Forgot password?</button>}</div></section></div>}
