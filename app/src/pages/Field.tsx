import { Camera, CheckCircle2, ClipboardList, Clock3, FileText, ShieldCheck } from 'lucide-react';
export function Field(){return <div className="page"><header className="page-header"><div><p className="eyebrow">Field</p><h1>Today on site</h1><p>Fast tools for crews without dashboard clutter.</p></div></header><div className="feature-grid">{[[Camera,'Photos','Capture progress, issues, before/after sets.'],[Clock3,'Time','Clock in by project, cost code, and task.'],[ClipboardList,'Daily log','Weather, manpower, work completed, blockers.'],[CheckCircle2,'Punch','Assign and close deficiencies with proof.'],[FileText,'Plans & docs','Current drawings, scopes, specs, and revisions.'],[ShieldCheck,'Safety','Toolbox talks, inspections, incidents, acknowledgements.']].map(([Icon,title,copy]:any)=><button className="feature-tile" key={title}><Icon/><strong>{title}</strong><span>{copy}</span></button>)}</div></div>}
