window.PULSELINK_CONFIG = {
  // Optional stable relay, for example: "https://toy-relay.example.com".
  // Invite links may also carry a relay URL, so this can remain blank for testing.
  relayBase: "",
  siteBase: "https://whatmod.com/toy"
};
